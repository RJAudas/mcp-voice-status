# -----------------------------
# Node / TypeScript (legacy - kept for reference)
# -----------------------------
node_modules/
dist/
build/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
coverage/
.nyc_output/

# Environment / secrets
.env
.env.*
!.env.example

# -----------------------------
# Logs / runtime artifacts
# -----------------------------
*.log
logs/
tmp/
temp/

# -----------------------------
# Voice Status state files (written to $env:TEMP at runtime, not tracked)
# These are listed here for documentation; they live in the system temp dir
# not in the repo, so no pattern needed. Kept as a note.
# $env:TEMP/voice-status-state.json
# -----------------------------

# -----------------------------
# Spec Kit
# Keep specs + memory tracked; ignore generated output
# -----------------------------
.specify/out/
.specify/tmp/
.specify/cache/

# -----------------------------
# VS Code / IDE
# -----------------------------
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json

# -----------------------------
# OS
# -----------------------------
.DS_Store
Thumbs.db
desktop.ini
